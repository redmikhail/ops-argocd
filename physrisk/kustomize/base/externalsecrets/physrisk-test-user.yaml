apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: physrisk-test-user
spec:
  secretStoreRef:
    name: osc-vault-store
    kind: SecretStore
  refreshInterval: "1h"
  target:
    name: physrisk-test-user
    creationPolicy: 'Owner'
    deletionPolicy: "Retain"
    template:
      engineVersion: v2
  dataFrom:
    - extract:
        key: osc/osc-cl2/physrisk/physrisk-test-user
